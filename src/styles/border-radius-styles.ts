import {StyleSheet} from 'react-native';

export const borderRadiusStyles = StyleSheet.create({
  border5: {
    borderRadius: 5,
  },
  border10: {
    borderRadius: 10,
  },
  border20: {
    borderRadius: 20,
  },
});
